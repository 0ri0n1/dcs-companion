[pytest]
testpaths = tests
pythonpath = ..
addopts = -ra
markers =
    local_integration: reads installed DCS, generated navigation caches, or the original user's bindings; requires DCS_LOCAL_INTEGRATION=1
